{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "npm test -w wallace",
    "test:canary": "cd canary && npm test",
    "build": "npm run build -w babel-plugin-wallace",
    "check": "babel $@",
    "prettier": "prettier \"**/*.{js,ts,jsx,tsx,json,md,yml,yaml}\" --write",
    "prettier:check": "prettier \"**/*.{js,ts,jsx,tsx,json,md,yml,yaml}\" --check",
    "publish": "npm whoami && lerna publish --no-private"
  },
  "workspaces": [
    "playground",
    "examples/*",
    "packages/*"
  ],
  "devDependencies": {
    "babel-jest": "^29.6.4",
    "html": "^1.0.0",
    "jest": "^29.6.4",
    "jest-fixed-jsdom": "^0.0.9",
    "jest-summary-reporter": "^0.0.2",
    "lerna": "^7.1.3",
    "prettier": "^3.8.0",
    "shx": "^0.3.4",
    "typescript": "^5.6.3"
  }
}
